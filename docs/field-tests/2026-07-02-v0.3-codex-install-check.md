# Field Test: Codex Install Check

Date: 2026-07-02

## Environment

- Agent surface: Codex desktop
- Skill path: `%USERPROFILE%\.codex\skills\powershell-command-runner`
- Repo: `agent-shells/powershell-skills`

## Scenario

Verify that the global Codex skill install exists after v0.3 and that the repo remains clean.

## Observed Behavior

- `C:\Users\12279\.codex\skills\powershell-command-runner\SKILL.md` exists.
- The global installer completed with `status: success`.
- The local git worktree was clean after install.

## Blocked Check

Starting a separate `codex` CLI process for a fresh-session auto-trigger test failed locally with WindowsApps access denied:

```text
Program 'codex.exe' failed to run ... WindowsApps ... Access denied.
```

This does not invalidate the installed skill, but it means the Codex fresh-session auto-trigger test still needs to be run through a normal Codex desktop session after restart or a working CLI entrypoint.

## Result

Partial.

## Follow-Up

Run a fresh Codex desktop session after restart and record whether a Windows path task automatically loads `powershell-command-runner`.
